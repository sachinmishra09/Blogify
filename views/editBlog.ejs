<!DOCTYPE html>
<html lang="en">

<head>

    <%- include('./partials/head.ejs') %>

    <title>Edit Blog</title>

    <!-- TinyMCE -->
    <script
        src="https://cdn.tiny.cloud/1/<%= tinymceApiKey %>/tinymce/8/tinymce.min.js"
        referrerpolicy="origin">
    </script>

    <style>
        body {
            background-color: rgb(120, 197, 225);
        }

        .edit-container {
            max-width: 1000px;
        }

        .edit-card {
            border: none;
            border-radius: 15px;
        }
    </style>

</head>


<body>

    <%- include('./partials/nav.ejs') %>


    <div class="container edit-container mt-4 mb-5">

        <div class="card edit-card shadow">

            <div class="card-body p-4 p-md-5">

                <h2 class="fw-bold mb-4">
                    Edit Blog
                </h2>


                <form
                    id="editBlogForm"
                    action="/blog/edit/<%= blog._id %>"
                    method="POST"
                >

                    <!-- Title -->

                    <div class="mb-4">

                        <label class="form-label fw-semibold">
                            Title
                        </label>

                        <input
                            type="text"
                            name="title"
                            class="form-control"
                            value="<%= blog.title %>"
                            required
                        >

                    </div>


                    <!-- Blog Content -->

                    <div class="mb-4">

                        <label class="form-label fw-semibold">
                            Blog Content
                        </label>


                        <!--
                            Do NOT put required here.
                            TinyMCE hides this textarea.
                        -->

                        <textarea
                            id="blogBody"
                            name="body"
                        ></textarea>


                        <!-- Existing blog content -->

                        <textarea
                            id="existingContent"
                            style="display: none;"
                        ><%= blog.body %></textarea>

                    </div>


                    <!-- Buttons -->

                    <button
                        type="submit"
                        class="btn btn-primary px-4"
                    >
                        Save Changes
                    </button>


                    <a
                        href="/blog/<%= blog._id %>"
                        class="btn btn-secondary px-4 ms-2"
                    >
                        Cancel
                    </a>

                </form>

            </div>

        </div>

    </div>


    <script>

        /*
         * Initialize TinyMCE
         */

        tinymce.init({

            selector: '#blogBody',

            height: 500,

            menubar: true,

            plugins: [
                'advlist',
                'autolink',
                'lists',
                'link',
                'charmap',
                'preview',
                'anchor',
                'searchreplace',
                'visualblocks',
                'code',
                'fullscreen',
                'insertdatetime',
                'media',
                'table',
                'wordcount'
            ],

            toolbar:
                'undo redo | ' +
                'blocks | ' +
                'bold italic underline strikethrough | ' +
                'forecolor backcolor | ' +
                'alignleft aligncenter alignright alignjustify | ' +
                'bullist numlist | ' +
                'outdent indent | ' +
                'link image media table | ' +
                'removeformat | ' +
                'code fullscreen',


            /*
             * Load existing blog content
             */

            setup: function (editor) {

                editor.on('init', function () {

                    const existingContent =
                        document.getElementById(
                            'existingContent'
                        ).value;

                    editor.setContent(existingContent);

                });

            },


            content_style: `
                body {
                    font-family: Arial, sans-serif;
                    font-size: 16px;
                    line-height: 1.6;
                    padding: 10px;
                }

                img {
                    max-width: 100%;
                    height: auto;
                }
            `

        });


        /*
         * Handle form submission
         */

        document
            .getElementById('editBlogForm')
            .addEventListener('submit', function (event) {

                const editor = tinymce.get('blogBody');

                /*
                 * Get content from TinyMCE
                 */

                const content = editor
                    .getContent()
                    .trim();


                /*
                 * Check if content is empty
                 */

                if (!content || content === '<p>&nbsp;</p>') {

                    event.preventDefault();

                    alert('Blog content cannot be empty.');

                    editor.focus();

                    return;

                }


                /*
                 * Copy TinyMCE content
                 * into the original textarea
                 */

                tinymce.triggerSave();

            });

    </script>


    <%- include('./partials/script.ejs') %>

</body>

</html>


<!-- Original code -->

<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <- include('./partials/head.ejs') %>
    <title>Edit Blog</title>
</head>
<body style="background-color: rgb(120, 197, 225);">
    <- include('./partials/nav.ejs') %>
    <div class="container mt-3">
        <form action="/blog/edit/<= blog._id %>" method="POST">
            <div class="mb-3">
                <label class="form-label">Title</label>
                <input type="text" name="title" class="form-control" value="<= blog.title %>" required>
            </div>
            <div class="mb-3">
                <label class="form-label">Body</label>
                <textarea name="body" class="form-control" rows="20" required><- blog.body %></textarea>
            </div>
            <button type="submit" class="btn btn-primary">Save changes</button>
        </form>
    </div>
</body>
</html> -->